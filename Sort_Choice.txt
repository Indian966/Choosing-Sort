import CHOOSING-SORT
unsorted = Heap_Sort

t = 0
print("원하시는 정수를 5개 입력해주세요:\n")
while t < 5:
    n = input()
    unsorted.append(n)
    t = t + 1


print("원하시는 정렬 방식이 있나요?\n")
print("1. 힙정렬  2. 버블정렬  3. 퀵정렬\n")
call = input()

if call == 1:
    Heap_Sort(unsorted)


def bubble_sort(unsorted):
    return unsorted

def quick_sort(unsorted):
    return unsorted

print("정렬 결과는 이렇습니다.\n")
print(unsorted)